# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build services-srv-callrecordings-gcloud-handler`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t services-srv-callrecordings-gcloud-handler`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system services-srv-callrecordings-gcloud-handler && \
          adduser --system -G services-srv-callrecordings-gcloud-handler services-srv-callrecordings-gcloud-handler

COPY dist/apps/services/srv-callrecordings-gcloud-handler services-srv-callrecordings-gcloud-handler
RUN chown -R services-srv-callrecordings-gcloud-handler:services-srv-callrecordings-gcloud-handler .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix services-srv-callrecordings-gcloud-handler --omit=dev -f install

CMD [ "node", "services-srv-callrecordings-gcloud-handler" ]
